# QA Playground Frontend

React + Vite frontend for the QA Playground project.

This app is intentionally small while the backend and automation flows are
being built out. It currently supports basic register and login flows against
the local backend API.

## Run

```bash
npm run dev
```

The frontend runs on `http://localhost:5173` by default.
